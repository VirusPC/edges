## 1. TypeScript MCP 服务骨架与接口定义

- [ ] 1.1 初始化 TypeScript + Node.js MCP server 工程结构与启动入口
- [ ] 1.2 定义 ingest 工具输入 schema（`title`、`content`、`coAuthor`）与必填校验
- [ ] 1.3 定义 ingest 工具输出 schema（`status`、`filePath`、`branch`、`prUrl`、`errorCode`）
- [ ] 1.4 实现统一错误模型与错误码映射（校验失败、git失败、push失败、凭据失败）

## 2. Ingest 编排与脚本适配

- [ ] 2.1 实现 ingest 编排流程（校验 -> 文件生成 -> commit -> push）并保证失败即停止下游步骤
- [ ] 2.2 实现 Node 层脚本调用适配（参数数组调用 `bin/new-note`，禁止 shell 字符串拼接）
- [ ] 2.3 改造 `bin/new-note` 以支持仓库路径可配置（例如环境变量覆盖），去除固定本地路径假设
- [ ] 2.4 实现脚本输出解析与结构化回传，确保成功/失败都返回机器可解析结果

## 3. Git 规则与安全约束落地

- [ ] 3.1 在服务层实现输入长度与格式安全约束，并补充边界值校验
- [ ] 3.2 落地分支命名规范与 commit metadata 校验，确保 co-author 透传
- [ ] 3.3 处理并发冲突策略（分支名冲突重试或唯一后缀）
- [ ] 3.4 增加外部依赖预检查（git/凭据/可选 gh）并输出明确诊断信息

## 4. 验证、文档与上线准备

- [ ] 4.1 编写单元测试：输入校验、错误码映射、结果结构化输出
- [ ] 4.2 编写集成测试：成功路径、参数缺失、git失败、push鉴权失败、PR依赖不可用
- [ ] 4.3 编写运行文档：环境变量、凭据配置、可选依赖（`gh`/`curl`/`python3`）
- [ ] 4.4 执行预发布验证并给出回滚方案（禁用 ingest tool 回退手工流程）
