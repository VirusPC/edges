export const LIST_AFTER_HELP = `
FLAGS
  --status <edges-tasks-status>  Filter: backlog | todo | in_progress | in_review | done | blocked | cancelled
  --json                          Write JSON to stdout (always on)

EXAMPLES
  edges tasks list
  edges tasks list --status in_progress
`;
