# Persistent /tasks/ board (ADR 0021). Generated HTML only.
# Do not proxy to 8787. Do not add /teach/ aliases.

location = /tasks {
    return 301 /tasks/;
}

location /tasks/ {
    alias __SITE_DIR__/;
    index index.html;
}
