1. kimi cli 不支持 两次 esc 清空 input。kimi code 两次esc类似 rewind的效果？
2. opencode 不支持rewind，只有 redo undo。kimi code 同样。
3. kimi code 多 agent 管理 & session 管理没 claude code 方便。claude code 可以方向键左键进入 session 管理。
4. kimi cli 不支持 !bash 命令输入。kimi code支持。
5. kimi cli ctrl+d 退出，反直觉。而且没必要在这种细节上和 claude code不一致，应该让用户更多关注功能。
6. claude code 可以拿到 cursor 光标选中的行之类的信息，牛逼
7. Claude code的代码一点也不优雅。公司还比较阴，比如说影响别人的缓存呀，比如说通过一些神奇手段记录用户区域呀。
8. Codex功能少
9. Claude code的steering取代了队列，切换不回去。有的时候，我突然想让它做另外一个小变更，但不中断当前任务。
10. 权限申请太频繁。
11. btw 只能答一个小问题，不能做复杂任务
12. 