# CLI 与直接 bash 写文件的本质区别

用户主动质疑 `obsidian create` 与 bash 直接写 markdown 的差异——说明用户有扎实的 bash/文件系统心智模型，是从「文件视角」进入这个主题的。澄清后的核心框架：纯写文本两者等价；CLI 的价值在 wikilink 名字解析、索引搜索、应用内一致性写入（无竞态）、结构化操作（property:daily:template）和 tasks/tags/backlinks 这类无 bash 等价物的视图。后续课程可直接用「文件视角 vs 应用视角」这对概念推进。
