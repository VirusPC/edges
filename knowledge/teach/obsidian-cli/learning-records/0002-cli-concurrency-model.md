# CLI 并发模型：命令级串行，跨命令无保护

用户主动追问并发写冲突——说明其使用场景天然是多 Agent（多终端）并发操作 vault。已建立的心智模型：CLI 命令经应用单线程事件循环串行执行（无文件损坏、create 有存在护栏、append 并发安全）；但跨命令读-改-写是 last-write-wins，静默丢写，无锁/事务/CRDT。实战守则：能 append 不重写、单笔记单写者、批量重写用 flock 包住、真合并交给 CRDT/git 层。后续第 2 课（Agent 实战）应直接按这些守则设计工作流，而不是事后补。
