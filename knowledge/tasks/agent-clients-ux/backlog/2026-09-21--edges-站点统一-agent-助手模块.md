---
name: edges_site_unified_agent_assistant
description: teaching、/tasks/ 等衍生站缺少统一的站内 agent 助手能力；后续要给整个 edges 站点提供可复用的 agent 助手模块。
metadata:
  edges-type: task
  edges-title: edges 站点统一 agent 助手模块
  edges-tasks-status: backlog
  edges-task-project: agent-clients-ux
  edges-updated-at: "2026-09-21T08:49:27.422Z"
---

结论（idea）：为整个 edges 衍生站点提供统一的 agent 助手模块（可嵌入/复用），而不是每站各写一套。

**事实背景:**
- 用户 peng cheng 2026-09-21：记录 todo，后续给整个 edges 的站点提供一个 agent 助手模块。
- 同机/同域已有或规划：teaching、Artifacts 预览、Tasks review 持久站、统一鉴权、tmp+persistent 入口等——站点数在增，助手若各站自建会重复。
- 相关但勿并：`agent-clients-ux` backlog「Electron 桌面端作为 edges 操作台」；`agent-clients-ux` backlog「评估Agent Plugins客户端支持」；`agent-clients-ux` backlog「edges 衍生站点统一鉴权」；`agent-clients-ux` backlog「Tasks review / review-page 写回仓接口」（交互不同层）。

**Why:**
teaching、/tasks/ 等衍生站缺少统一的站内 agent 助手能力；后续要给整个 edges 站点提供可复用的 agent 助手模块。

**How to apply:**
- grill 能力边界（问答/改看板/导航）、与鉴权/写回接口关系、嵌入形态。
- 派发默认先 grill-with-docs，过关再实现。
- 未指派。
